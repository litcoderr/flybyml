project: rl
run: ddpg_v1
device: cuda
step_interval: 0.1
train:
  batch_size: 32
  total_steps: 100000000
  reset_period: 1000
  start_steps: 1000 # TODO needs to be changed to a larger value
  act_noise: 0.1
  rew_scale: 1.0
  replay_size: 1000000
model:
  obs_dim: 12 # [e_pitch, e_roll, e_spd, traj_pitch, traj_roll, traj_spd, d_roll, d_pitch, d_spd, i_roll, i_pitch, i_spd]
  act_dim: 3 # [d_elev, d_ail, d_thrust]