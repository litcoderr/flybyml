project: simple_fc
run: batch_normalize
seed: 42
train:
  batch_size: 64
  num_workers: 4
  gpus: 1
  num_epochs: 100000
  save_epochs: 500
  plot_every_epochs: 500
test:
  batch_size: 64
  num_workers: 4
model:
  dsensory: 4
  dinst: 4
  daction: 10
dataset:
  root: /data/flybyml_dataset_v1